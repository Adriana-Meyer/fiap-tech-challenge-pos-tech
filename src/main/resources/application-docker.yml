spring:
  datasource:
    url: jdbc:mysql://mysql:3306/workshop_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: workshop
    password: workshop123

  jpa:
    show-sql: false

  flyway:
    enabled: true

logging:
  level:
    com.fiap.workshop: INFO
