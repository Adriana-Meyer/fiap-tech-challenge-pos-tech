apiVersion: v1
kind: Service
metadata:
  name: workshop-app
  namespace: workshop
spec:
  type: NodePort
  selector:
    app: workshop-app
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
