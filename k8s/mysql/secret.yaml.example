# Template de referência — NÃO é aplicado diretamente.
# O Secret real é criado via `kubectl create secret generic` (comando documentado
# no README/plano da Etapa 4), nunca commitado com valores reais.
apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: workshop
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: CHANGE_ME
  MYSQL_PASSWORD: CHANGE_ME
